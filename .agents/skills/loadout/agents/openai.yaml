interface:
  display_name: "Loadout"
  short_description: "Repo-specific patterns and workflows for loadout"
  default_prompt: "Use the loadout repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true